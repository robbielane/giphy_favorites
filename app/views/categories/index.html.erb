<h3> Categories </h3>

<div class="row">
  <% @categories.each do |cat| %>
  <div class="col s4 gifs">
    <%= link_to cat.name, category_path(cat) %><br>
    <%= link_to image_tag(cat.gifs.first.url), category_path(cat) %>
  </div>
  <% end %>
</div>
